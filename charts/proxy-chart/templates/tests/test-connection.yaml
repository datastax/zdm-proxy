apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proxy-chart.fullname" . }}-test-connection"
  labels:
    {{- include "proxy-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proxy-chart.fullname" . }}:{{ .Values.proxyConfig.proxyMetricsPort }}']
  restartPolicy: Never
